---
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: ca-app-issuer
  labels:
    app: "ca-cert"
    release: {{ $.Release.Name | quote }}
    chart: {{ $.Chart.Name }}
spec:
  ca:
    secretName: {{ .Values.certsecretName }}
